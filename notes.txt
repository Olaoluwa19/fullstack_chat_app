bun.com
expo.dev

backend - bun init -y
web - bun create vite .
bunx create-expo-app@latest .

rm -rf .git
git init
git add .
git commit -m "initial commit"

bun add express cors mongoose socket.io @clerk/express @types/express

TO USE CLERK FOR AUTH IN PROJECT:
go to clerk.com, 
create an application and give it a name
run bun add @clerk/react for your frontend(web),
set your Clerk API keys
then continue following the steps